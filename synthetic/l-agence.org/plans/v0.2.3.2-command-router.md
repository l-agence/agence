# Phase 1.5: v0.2.3.2 — Command Router Implementation

**Status**: ⏳ **QUEUED** (Next in line)  
**Owner**: Haiku (this session)  
**Estimate**: 1–2 weeks

---

## 📌 Objective

Implement 8 command primitives for managing notes, logs, plans, todos, faults, issues, tasks, and jobs. Establish routing inheritance model and shell completion.

---

## 🎯 Deliverables

| Command | Scope | Backend | Implementation |
|---------|-------|---------|-----------------|
| `^lesson [list\|show\|add]` | synthetic/@/lessons/ | Existing module | Routing + completion |
| `^log [list\|show\|add]` | nexus/logs/ | NEW | Structured logging |
| `^plan [list\|show\|add]` | synthetic/@/plans/ | NEW (just created) | Router + index.md parser |
| `^todo [list\|show\|add]` | hermetic/@/todos/ | NEW | Local-only (never shared) |
| `^fault [list\|show]` | nexus/faults/ | NEW | Already in NEXUS |
| `^issue [list\|show\|add]` | synthetic/@/issues/ | NEW | Team-visible |
| `^task [list\|show\|add]` | organic/tasks/ | NEW | Matrix math prep |
| `^job [list\|show\|add]` | organic/jobs/ | NEW | Agent-routable |

---

## 🔧 Technical Requirements

### **Routing Inheritance**
```bash
^plan list
  → Check @current_context (default: @)
  → Fallback: synthetic/@/plans/
  
^plan list --org acme.tld
  → Explicit: synthetic/@acme.tld/plans/
  → Fallback: synthetic/@/plans/
```

### **Shell Completion**
```bash
^plan [list|show|add] [--org NAME]
^todo [list|show|add]
^lesson [list|show|add] [--org NAME]
# etc.
```

### **Local-Only Enforcement** (hermetic/@)
```bash
# ^todo is ALWAYS local (never committed)
^todo list  → hermetic/@/todos/  (only)
^todo add "Refactor lib.sh"  → ~/.agence/hermetic/@/todos/
```

---

## 📁 Files to Create/Modify

- [ ] `bin/^` router expansion (8 new commands)
- [ ] `lib/command-router.sh` (routing logic common to all)
- [ ] `lib/formatter.sh` expansion (index.md + JSON parsing)
- [ ] `.gitignore` update (hermetic/@/todos/ exclusion)
- [ ] `codex/COMMANDS.md` (command reference)
- [ ] Shell completion scripts (bash/zsh/pwsh)
- [ ] Tests: `tests/unit/command-router_spec.sh`

---

## 🧪 Test Cases

- [ ] `^lesson list` (existing lessons from synthetic/@/lessons/)
- [ ] `^plan show "v0.2.3-stabilization"` (from just-created plans/)
- [ ] `^todo add "Test command router"` (creates in hermetic/@/todos/, not committed)
- [ ] `^todo list --owner steff` (filters by owner)
- [ ] `^task list` (empty until organic/tasks/ populated)
- [ ] `^plan list --org acme.tld` (inheritance + fallback)

---

## 📚 References

- [COMMANDS.md](codex/COMMANDS.md) — Command reference (to be enhanced)
- [TAXONOMY.md](codex/TAXONOMY.md) — Scopes and routing rules
- [SYMBOLS.md](synthetic/l-agence.org/docs/SYMBOLS.md) — Symbol parsing

---

**Next Phase**: v0.2.4 Docker + Matrix Math  
**Handoff Candidate**: If Haiku exhausted, hand off to Sonnet mid-phase
