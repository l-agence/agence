# Phase 3: v0.3.0 — VSCode Tiles & Real-Time Job Control

**Status**: 📍 **QUEUED** (After Phase 2)  
**Owner**: Sonnet (VSCode extension complexity)  
**Estimate**: 3–4 weeks

---

## 📌 Objective

Implement 2-column tile layout in VSCode: left (human console, full authority) + right (aibash/aishell, observable). Enable POSIX job control as coordination primitive.

---

## 🎯 Key Deliverables

- [ ] VSCode extension: N agents = N rows, 2 columns per row
- [ ] Left tile: `docker exec -it` (human overrides allowed)
- [ ] Right tile: aibash/aishell subprocess (agent execution)
- [ ] Ctrl+K hotkey: SIGKILL aibash (human stops agent)
- [ ] `%jobs` integration: show all running tasks
- [ ] Session capture: both tiles logged + linked
- [ ] Signal propagation: parent shell → overrides child

---

## 🔧 Technical Highlights

**Job Control Model**:
```bash
docker run -it container /bin/bash
  ├─ aibash &                  (background, right tile)
  ├─ fg %1                     (bring to foreground if needed)
  ├─ Ctrl+Z → SIGSTOP         (pause aibash)
  └─ Ctrl+K → SIGKILL         (kill aibash, restart if needed)

%jobs output:
[1]   Running     aibash
[2]   Stopped     make test
[3]-  Running     ./deploy.sh
```

**Tile Layout (Mock)**:
```
┌─ Agent: ralph ─────────────────┬─ ralph's aibash ────────────────┐
│ (human can type here)          │ (agent executing tasks)         │
│ > ls -la                       │ $ task-001: optimizing...      │
│ > docker exec ... /bin/bash    │ $ task-001: 45% complete       │
│ >                              │                                 │
├────────────────────────────────┼─────────────────────────────────┤
│ Agent: aider                   │ aider's aishell                 │
│ > Ctrl+K                       │ $ task-002: running tests...   │
│ > kill %aibash (sent)          │ $ task-002: ✓ test_api.sh      │
├────────────────────────────────┼─────────────────────────────────┤
│ Shared Console                 │ Session Metadata                │
│ > ^plan list                   │ {                               │
│ (global commands)              │   "ralph": { ... },            │
└────────────────────────────────┴─────────────────────────────────┘
```

---

## 📁 Files to Create/Modify

- [ ] VSCode extension: manifest + entry
- [ ] Extension: Tile renderer (2-column layout)
- [ ] Extension: Left terminal control (docker subprocess)
- [ ] Extension: Right terminal control (aibash monitoring)
- [ ] Extension: Hotkey binding (Ctrl+K → SIGKILL)
- [ ] aibash: Signal handler for graceful shutdown
- [ ] Tests: Signal propagation + tile synchronization

---

## 🎓 Key Principles

- **Human authority**: Left tile always dominates (overrides allowed)
- **Observability**: Both tiles visible simultaneously (real-time feedback)
- **Isolation**: Each agent in separate container (no cross-agent pollution)
- **Coordination**: POSIX job control (no custom protocol)

---

**Blocks**: Phase 2 must be complete  
**Next Phase**: v0.3.1 Orchestrator  
**Candidate for**: Sonnet (VSCode API complexity)
