#!/bin/bash
# Usage: link <source_dir> <link_dir>
if [ "$#" -ne 2 ]; then
  echo "Usage: link <source_dir> <link_dir>"
  exit 1
fi
SOURCE="$1"
LINK="$2"
cmd.exe /c mklink /j "$(cygpath -w "$LINK")" "$(cygpath -w "$SOURCE")"