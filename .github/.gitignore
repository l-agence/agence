# Agence core patterns (auto-merged by ^init)
# These patterns ensure agence framework files are properly ignored

# Agence runtime artifacts
.aisecrets/
.aisession_cache/

# Session logs and state (persisted per repo)
nexus/.ai*/

# Local overrides / secrets
.mysecrets/
.private/

# Generated/transient files
@
Lib
Scripts
CLAUDE.md
.ai.md

# Framework metadata (generated by ^init)
.github/copilot-instructions.md

# User-specific patterns (preserve these)
# Add site-specific ignores below this line without removing above patterns
